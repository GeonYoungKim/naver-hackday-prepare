<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">

	<select id="selectList" parameterType="java.util.Map" resultType="hashmap">
		SELECT notice.num,notice.content
		FROM naver_hack.notice notice, naver_hack.notice_element ne
		WHERE notice.num=ne.num and (
		<foreach collection="userElement" item="item" separator=" or ">
            ne.notice_element=#{item.element}
        </foreach>
        )
		GROUP BY notice.num ORDER BY notice.num DESC LIMIT #{no},#{unit};
	</select>
	
	<insert id="insert" parameterType="com.hack.naver.model.Notice" useGeneratedKeys="true" keyProperty="num"  keyColumn="num">
		INSERT INTO 
			naver_hack.notice (content, user_id) 
		VALUES
			(#{content},#{id});
		<selectKey keyProperty="num" resultType="Integer" order="AFTER">
	        SELECT LAST_INSERT_ID()
	    </selectKey>
		
	</insert>
</mapper>